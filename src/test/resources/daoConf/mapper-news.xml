<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper.TestMapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="common.mapper.NewsMapper">
    <select id="read" resultType="common.pojos.NewsData">
        select * from NEWS_DATA where id=${id}
    </select>

    <insert id="create" parameterType="common.pojos.NewsData" >
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select news_data_seq.nextval from dual
        </selectKey>
        INSERT INTO NEWS_DATA
        (id, title, content, md5, inserttime, category_code, search_keyword, url, pubtime, brief, source, same_url, same_num, img_url)
        VALUES
        (#{id},#{title},#{content},#{md5},sysdate,0,#{searchKey},#{url},#{pubdate},#{brief},#{source},#{sameUrl},#{sameNum},#{imgUrl})
    </insert>

</mapper>
