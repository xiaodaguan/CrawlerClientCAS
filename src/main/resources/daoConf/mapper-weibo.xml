<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper.TetMapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="common.mapper.WeiboMapper">

    <resultMap id="BaseResultMap" type="common.pojos.WeiboData">
        <result column="pubtime" jdbcType="TIMESTAMP" property="pubtime" javaType="java.util.Date" />
    </resultMap>

    <select id="read" resultType="common.pojos.WeiboData">
        select * from Weibo_DATA where id=${id}
    </select>



    <insert id="create" parameterType="common.pojos.WeiboData" >
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select weibo_data_seq.nextval from dual
        </selectKey>
        INSERT INTO Weibo_DATA
        (search_keyword,md5,content,author,author_url,insert_time,
        source,img_url,author_img,url,site_id,pubtime,rtt_count,
        comment_count, rtt_url,comment_url,mid,category_code,user_id,
        gps)
        VALUES
        (#{searchKey},#{md5}, #{content}, #{author}, #{authorurl}, sysdate,
        #{source}, #{imgUrl},#{authorImg}, #{url},#{siteId},#{pubdate},#{rttNum},
        #{commentNum}, #{rttUrl}, #{commentUrl}, #{mid}, #{categoryCode},  #{dataId},
        #{contentAddress})
        <!--#{pubtime,jdbcType=TIMESTAMP}-->
    </insert>
</mapper>
