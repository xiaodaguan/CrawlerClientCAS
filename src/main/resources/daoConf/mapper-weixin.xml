<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper.TestMapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="common.mapper.WeixinMapper">
    <select id="read" resultType="common.pojos.WeixinData">
        select * from WEIXIN_DATA where id=${id}
    </select>

    <insert id="create" parameterType="common.pojos.WeixinData" >
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select weixin_data_seq.nextval from dual
        </selectKey>
        INSERT INTO WEIXIN_DATA
        (id, title, author, pubtime, source, url, inserttime, search_keyword, md5, content, brief, img_url, read_num, like_num, updated)
        VALUES
        (#{id}, #{title}, #{author}, #{pubdate}, #{source}, #{url}, sysdate, #{searchKey}, #{md5}, #{content}, #{brief}, #{imgUrl}, #{readNum}, #{praiseNum}, 0)
    </insert>

</mapper>
