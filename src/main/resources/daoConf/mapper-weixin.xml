<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD baseMapper.TestMapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="common.mapper.WeixinMapper">
    <select id="read" resultType="common.pojos.WeixinData">
        select * from WEIXIN_DATA where id=${id}
    </select>

</mapper>
